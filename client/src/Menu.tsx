function Menu() {
	// useEffect(() => {
	// 	if (isPlaying) {
	// 		socket.emit("join_game", "ready");

	// 		socket.on("game_status", (arg) => {
	// 			console.log(arg);
	// 		});
	// 	}
	// }, [isPlaying]);

	return <button>Join Game!</button>;
}

export default Menu;
